This App is for Linux Systems.

To run it you need to do a few steps:

1. Create a venv via: python3 -m venv venv
2. start the venv: source venv/bin/activate
3. install requirements: pip install pyside6
4. run the app (to see all information that could be provided, you should run the main.py with sudo privileges)

